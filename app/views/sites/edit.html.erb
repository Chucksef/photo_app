<% provide(:title, 'Site Options') %>

<div class="page-header">
    <h1>Site Options</h1>
</div>

<div class="col-md-4 col-md-offset-4 col-sm-12">
    <%= form_for @site, url: options_path do |f| %>
        
        <%= render 'shared/error_messages' %>

        <div class="form-group">
            <%= f.label "Name"%>
            <%= f.text_field :name, class: "form-control" %>
        </div>

        <%= f.submit "Update Site", class: "btn btn-block btn-primary" %>

    <% end %>
</div>